// Referenced through `types: ["@perseid/dev-kit/types"]` rather than listing packages directly:
// TypeScript then resolves them from the dev-kit's real location, which pnpm's layout requires.
/// <reference types="node" />
/// <reference types="vitest/globals" />
