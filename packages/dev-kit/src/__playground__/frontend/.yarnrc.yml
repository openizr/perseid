nodeLinker: node-modules

# Dependencies are installed from Docker (linux) but IDE tooling (ESLint) runs on the host:
# native bindings are needed for both platforms.
supportedArchitectures:
  os: [current, darwin, linux]
  cpu: [current, arm64, x64]
  libc: [current, glibc, musl]
